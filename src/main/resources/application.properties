spring.application.name=taskmanager

jwt.secret=${JWT_SECRET:42a63ff70cf28acb532283668ee40ab7}

# Use environment variable for database URL if available, otherwise use local
# environmental
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# local
# spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/taskmanager}
# spring.datasource.username=${DB_USERNAME:your_username}
# spring.datasource.password=${DB_PASSWORD:your_password}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Required for deployment
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.port=${PORT:8080}